pipeline {
   agent { label 'master' }
   stages {
       stage('read') {
           steps {
               script {
                   def data = readFile(file: 'SetupTemplateUpdateDBs.ps1')
                   println(data)
               }
           }
       }
   }
}
